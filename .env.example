# ========================================
# iNephro 環境變數設定範例
# ========================================

# ========================================
# 本地開發環境變數（僅用於開發伺服器）
# ========================================

# Upstash Redis - 雲端快取（選填）
# 從 https://console.upstash.com/ 建立資料庫後取得
# 注意：本地開發時可以選填，Vercel 部署時必須在 Dashboard 設定
UPSTASH_REDIS_REST_URL=https://your-redis-url.upstash.io
UPSTASH_REDIS_REST_TOKEN=your-upstash-token-here

# ⚠️ 重要安全提醒 ⚠️
# OpenAI API Key 和 Assistant ID 只能在後端使用！
# 請在 Vercel Dashboard → Settings → Environment Variables 設定：
#   - OPENAI_API_KEY (不要加 VITE_ 前綴)
#   - ASSISTANT_ID (不要加 VITE_ 前綴)
#
# 本地開發時，這些變數會透過 Edge Function 從環境變數讀取，
# 不需要在 .env.local 設定


# ========================================
# 設定步驟
# ========================================
# 1. 複製此檔案為 .env.local
# 2. 填入你的 OpenAI API Key
# 3. (選填) 如要使用知識庫模式，填入 Assistant ID
# 4. (選填) 如要啟用雲端快取，填入 Upstash Redis 連線資訊
# 5. 重新啟動開發伺服器: npm run dev
#
